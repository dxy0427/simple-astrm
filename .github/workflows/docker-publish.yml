name: 手动构建 simple-astrm 镜像到 GHCR

on:
  workflow_dispatch:
    inputs:
      image-tag:
        description: '镜像标签（默认：latest）'
        required: false
        default: 'latest'

jobs:
  build-and-push-ghcr:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: 检查代码
        uses: actions/checkout@v4

      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 登录 GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 提取镜像标签
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/${{ github.actor }}/simple-astrm
          tags: |
            ${{ github.event.inputs.image-tag }}
            ${{ github.sha }}
          flavor: |
            latest=${{ github.event.inputs.image-tag == 'latest' }}

      - name: 构建并推送镜像
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: 输出镜像信息
        run: |
          echo "✅ 镜像构建推送完成！"
          echo "📦 镜像地址：${{ steps.meta.outputs.tags }}"
          echo "💡 拉取命令：docker pull ghcr.io/${{ github.actor }}/simple-astrm:${{ github.event.inputs.image-tag }}"
          echo "🟢 运行命令：docker run -d -p 8095:8095 -v \$(pwd)/config.yaml:/app/config.yaml --restart always ghcr.io/${{ github.actor }}/simple-astrm:${{ github.event.inputs.image-tag }}"